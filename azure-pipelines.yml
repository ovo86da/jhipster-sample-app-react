trigger:
  branches:
    include:
      - main

pr:
  branches:
    include:
      - main

pool:
  name: Default

variables:
  JAVA_VERSION: '21'
  MAVEN_CACHE_FOLDER: '$(Pipeline.Workspace)/.m2/repository'
  NPM_CACHE_FOLDER: '$(Pipeline.Workspace)/.npm'
  MAVEN_OPTS: '-Dmaven.repo.local=$(MAVEN_CACHE_FOLDER)'

stages:
  - stage: build_and_package
    displayName: 'Build, test, package and publish artifacts'
    jobs:
      - job: build
        displayName: 'Build JHipster React app'
        steps:
          - checkout: self
            clean: true
            fetchDepth: 1
            persistCredentials: false

          - script: |
              java -version
              node -v
              npm -v
              ./mvnw -version
            displayName: 'Verify toolchain'

          - task: Cache@2
            displayName: 'Cache Maven repository'
            inputs:
              key: 'maven | "$(Agent.OS)" | pom.xml'
              restoreKeys: |
                maven | "$(Agent.OS)"
              path: '$(MAVEN_CACHE_FOLDER)'

          - task: Cache@2
            displayName: 'Cache npm cache'
            inputs:
              key: 'npm | "$(Agent.OS)" | package-lock.json'
              restoreKeys: |
                npm | "$(Agent.OS)"
              path: '$(NPM_CACHE_FOLDER)'

          - script: |
              set -euo pipefail
              chmod +x mvnw
              [ -f npmw ] && chmod +x npmw || true
              ./mvnw -B -ntp -Pprod clean verify
            displayName: 'Build and test with Maven (prod profile)'
            env:
              MAVEN_OPTS: $(MAVEN_OPTS)

          - script: |
              set -euo pipefail
              ./mvnw -B -ntp org.cyclonedx:cyclonedx-maven-plugin:2.9.1:makeAggregateBom \
                -DskipTests \
                -DoutputFormat=json \
                -DoutputName=sbom
            displayName: 'Generate CycloneDX SBOM'
            env:
              MAVEN_OPTS: $(MAVEN_OPTS)